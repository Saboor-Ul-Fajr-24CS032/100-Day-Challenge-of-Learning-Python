Day 87 -> Creating RadioButtons

A Radiobutton in Tkinter is a widget that allows the user to select one option from a set of choices. 
All radiobuttons in the group share the same control variable (e.g., IntVar or StringVar), and each has a unique value. 
When a radiobutton is selected, the control variable is set to its value, which can be retrieved using .get().

